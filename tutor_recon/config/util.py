"""Utilities for working with resource claims."""

from typing import Any
from tutor_recon.util.vjson.util import walk_dict


def claim_ids_from_settings(prefix: str, settings: dict, claim_holder: Any) -> dict:
    return {tuple([])}
